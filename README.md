# google-auth

SuperTokens (EmailPassword) + Express + MongoDB example.

## Setup

1. `npm install`
2. Create `.env` with:
   - `SUPERTOKENS_CONNECTION_URI`
   - `SUPERTOKENS_API_KEY`
   - `MONGODB_URI`
   - `PORT` (optional, default 3000)
3. `npm run dev`
4. Open http://localhost:3000

## Scripts

- `npm run build` — bundle `src/auth.js` → `public/auth.js` with esbuild
- `npm run watch` — rebuild on change
- `npm run dev`   — build, then start server
- `npm start`     — same as dev
<img width="552" height="507" alt="image" src="https://github.com/user-attachments/assets/8b3a7b3c-2d69-4078-b557-50553c7ca93d" />

<img width="1843" height="771" alt="image" src="https://github.com/user-attachments/assets/b9fda7d5-32d3-4b09-a9c3-4c1e3aa34397" />
